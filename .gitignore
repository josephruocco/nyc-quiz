dist/
.claude/

# Raw source extracts; re-fetch with the curls in the README. The built *_data.json
# files are the committed artifacts, since Netlify's build needs them and they're
# far smaller (subway_lines.json alone is 8.8MB).
nta.geojson
subway_lines.json
subway_stations.json
